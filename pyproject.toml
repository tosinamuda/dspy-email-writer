[project]
name = "dspy-email-writer"
version = "0.1.0"
description = "Companion code for 'Can't Get Enough of DSPy' — without-DSPy vs DSPy side by side"
requires-python = ">=3.10"
dependencies = [
    "dspy>=3.0",  # GEPA ships in 3.x
    "openai>=1.0",  # used by without_dspy.py
    "pandas>=2.0",
]
